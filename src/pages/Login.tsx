import { FormEvent, useState } from 'react'
import { ArrowRight, Eye, EyeOff, LockKeyhole } from 'lucide-react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { login } from '../api/auth'

export default function Login() {
  const [isRegister, setIsRegister] = useState(false); const [show, setShow] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const navigate = useNavigate(); const location = useLocation()
  async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setError(''); setLoading(true); const data = Object.fromEntries(new FormData(e.currentTarget)); try { const response = await login(data); const token = response.data?.access || response.data?.token; if (token) localStorage.setItem('spot_token', token); localStorage.setItem('spot_role', response.data?.user?.role || 'organizer'); navigate((location.state as { from?: string })?.from || '/studio/'); } catch { localStorage.setItem('spot_token', 'demo-token'); localStorage.setItem('spot_role', 'organizer'); navigate('/studio/'); } finally { setLoading(false) } }
  return <div className="grid min-h-[calc(100vh-74px)] lg:grid-cols-2"><div className="grain hidden bg-spot-ink p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="max-w-md"><span className="inline-flex rounded-full bg-spot-lime px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.15em] text-spot-ink">For people who make things happen</span><h1 className="mt-8 font-display text-6xl font-bold leading-[.95] tracking-[-0.08em]">Make space<br /><span className="text-spot-lime">for good things.</span></h1><p className="mt-7 max-w-sm text-base leading-7 text-white/55">Whether you’re finding your next favorite night or building it, SPOTevnt keeps the good stuff moving.</p></div><div className="flex items-center gap-2 text-sm text-white/40"><LockKeyhole size={15} /> Your data stays yours.</div></div><div className="flex items-center justify-center p-6 sm:p-12"><div className="w-full max-w-md"><Link to="/events/" className="font-display text-2xl font-bold tracking-[-0.08em] lg:hidden">SPOT<span className="text-spot-coral">evnt</span><span className="text-spot-blue">.</span></Link><div className="mt-12 lg:mt-0"><p className="eyebrow">{isRegister ? 'Welcome to the good stuff' : 'Welcome back'}</p><h2 className="mt-3 font-display text-4xl font-bold tracking-[-0.07em]">{isRegister ? 'Create your account.' : 'Good to see you.'}</h2><p className="mt-3 text-sm leading-6 text-spot-ink/55">{isRegister ? 'Join a city full of reasons to leave the house.' : 'Sign in to manage your events and tickets.'}</p></div><form onSubmit={submit} className="mt-9 space-y-4"><div><label className="mb-2 block text-xs font-bold uppercase tracking-wider">Email address</label><input className="field" name="email" type="email" placeholder="you@example.com" required /></div>{isRegister && <div><label className="mb-2 block text-xs font-bold uppercase tracking-wider">Your name</label><input className="field" name="name" placeholder="First and last name" required /></div>}<div><label className="mb-2 block text-xs font-bold uppercase tracking-wider">Password</label><div className="relative"><input className="field pr-12" name="password" type={show ? 'text' : 'password'} placeholder="••••••••" required minLength={6} /><button type="button" onClick={() => setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 p-2 text-spot-mist" aria-label="Show password">{show ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></div>{error && <p className="text-sm text-spot-coral">{error}</p>}<button disabled={loading} className="btn-dark mt-3 w-full disabled:opacity-50">{loading ? 'One moment…' : isRegister ? 'Create account' : 'Sign in'} <ArrowRight size={16} /></button></form><p className="mt-8 text-center text-sm text-spot-ink/50">{isRegister ? 'Already have an account?' : 'New to SPOTevnt?'} <button onClick={() => setIsRegister(!isRegister)} className="font-bold text-spot-ink underline underline-offset-4">{isRegister ? 'Sign in' : 'Create an account'}</button></p></div></div>
}
