import { useState } from 'react'
import { ArrowLeft, CalendarDays, Check, Clock3, MapPin, Minus, Plus, Share2, Users } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import EventCard from '../components/EventCard'
import { Event } from '../components/EventCard'

const event: Event = { id: 1, slug: 'the-sunday-reset', title: 'The Sunday Reset', date: 'Sunday, 16 June 2025', time: '10:00 AM – 1:00 PM', location: 'The Garden Room, Dublin 8', city: 'Dublin', category: 'Wellness', price: '€18', image: 'https://images.unsplash.com/photo-1545205597-3d9d02c29597?auto=format&fit=crop&w=1500&q=90', description: 'A soft landing into the week ahead. The Sunday Reset is three hours of movement, journaling, seasonal food and the kind of conversations that stay with you. Come exactly as you are.', organizer: 'Sunday Society', attendees: '84 going' }

export default function EventDetail() {
  useParams(); const [quantity, setQuantity] = useState(1)
  return <div><div className="container-page py-8"><Link to="/events/" className="inline-flex items-center gap-2 text-sm font-bold text-spot-ink/55 hover:text-spot-ink"><ArrowLeft size={16} /> Back to events</Link></div><section className="container-page grid gap-10 pb-20 lg:grid-cols-[1.15fr_.85fr] lg:items-start"><div><div className="aspect-[4/3] overflow-hidden rounded-[28px] bg-spot-panel sm:aspect-[16/10]"><img src={event.image} alt="" className="h-full w-full object-cover" /></div><div className="mt-10 max-w-2xl"><div className="flex flex-wrap items-center gap-3"><span className="rounded-full bg-spot-coral/10 px-3 py-1.5 text-[11px] font-bold uppercase tracking-[0.12em] text-spot-coral">{event.category}</span><span className="text-sm text-spot-ink/45">{event.attendees}</span></div><h1 className="mt-5 font-display text-5xl font-bold leading-[.96] tracking-[-0.07em] sm:text-7xl">{event.title}</h1><p className="mt-7 text-lg leading-8 text-spot-ink/65">{event.description}</p><div className="mt-10 grid gap-5 border-y border-spot-ink/10 py-6 sm:grid-cols-2"><Info icon={<CalendarDays size={18} />} label="When" value={event.date} /><Info icon={<Clock3 size={18} />} label="Time" value={event.time!} /><Info icon={<MapPin size={18} />} label="Where" value={event.location} /><Info icon={<Users size={18} />} label="Hosted by" value={event.organizer!} /></div><div className="mt-10 flex items-center justify-between"><div><p className="eyebrow">Hosted by</p><p className="mt-2 font-display text-xl font-bold">Sunday Society</p><p className="mt-1 text-sm text-spot-ink/50">Making space for softer Sundays.</p></div><button className="btn-outline px-4" aria-label="Share event"><Share2 size={17} /> <span className="hidden sm:inline">Share</span></button></div></div></div><aside className="sticky top-28 rounded-[24px] bg-white p-6 shadow-soft sm:p-8"><p className="eyebrow">Get your spot</p><h2 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em]">Make it a plan.</h2><div className="mt-7 flex items-center justify-between border-b border-spot-ink/10 pb-6"><span className="text-sm text-spot-ink/55">General admission</span><span className="font-display text-2xl font-bold">€18</span></div><div className="flex items-center justify-between py-6"><span className="text-sm font-semibold">How many?</span><div className="flex items-center gap-4 rounded-full border border-spot-ink/10 px-2 py-1"><button onClick={() => setQuantity(Math.max(1, quantity - 1))} className="rounded-full p-1 hover:bg-spot-cream"><Minus size={15} /></button><span className="w-4 text-center text-sm font-bold">{quantity}</span><button onClick={() => setQuantity(Math.min(8, quantity + 1))} className="rounded-full p-1 hover:bg-spot-cream"><Plus size={15} /></button></div></div><button className="btn-dark w-full">Continue to checkout <span className="ml-auto">€{18 * quantity}</span></button><div className="mt-6 flex gap-2 text-xs leading-5 text-spot-ink/45"><Check size={15} className="mt-0.5 shrink-0 text-spot-blue" /> Instant confirmation. Your ticket will be ready in your inbox.</div></aside></section></div>
}

function Info({ icon, label, value }) { return <div className="flex gap-3"><span className="mt-0.5 text-spot-blue">{icon}</span><div><p className="eyebrow">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div></div> }
