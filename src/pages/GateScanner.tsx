import { useState } from 'react'
import { Camera, Check, ChevronDown, Keyboard, ScanLine, ShieldCheck, X } from 'lucide-react'

export default function GateScanner() {
  const [status, setStatus] = useState<'idle' | 'valid' | 'invalid'>('idle'); const [code, setCode] = useState('')
  function verify() { setStatus(code.trim().toLowerCase().includes('spot') ? 'valid' : 'invalid') }
  return <div className="min-h-[calc(100vh-74px)] bg-spot-panel text-white"><div className="mx-auto max-w-5xl px-5 py-8 sm:px-8 sm:py-12"><div className="flex items-center justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-white/40">Gate scanner</p><h1 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em]">The Sunday Reset</h1></div><button className="flex items-center gap-2 rounded-full border border-white/15 px-4 py-2 text-sm font-semibold text-white/65">Main gate <ChevronDown size={15} /></button></div><div className="mt-12 grid gap-8 lg:grid-cols-[1fr_310px]"><section><div className="relative aspect-[4/3] overflow-hidden rounded-[28px] border border-white/10 bg-spot-ink sm:aspect-[16/9]"><div className="absolute inset-8 rounded-2xl border-2 border-spot-lime/75 sm:inset-20"><span className="absolute -left-0.5 -top-0.5 h-7 w-7 border-l-4 border-t-4 border-spot-lime" /><span className="absolute -right-0.5 -top-0.5 h-7 w-7 border-r-4 border-t-4 border-spot-lime" /><span className="absolute -bottom-0.5 -left-0.5 h-7 w-7 border-b-4 border-l-4 border-spot-lime" /><span className="absolute -bottom-0.5 -right-0.5 h-7 w-7 border-b-4 border-r-4 border-spot-lime" /></div><div className="absolute inset-x-0 bottom-7 flex justify-center"><span className="rounded-full bg-spot-ink/80 px-4 py-2 text-xs font-semibold text-white/70 backdrop-blur">Point camera at QR code</span></div><div className="absolute left-1/2 top-1/2 grid h-16 w-16 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-spot-lime text-spot-ink"><ScanLine size={28} /></div></div><div className="mt-5 flex items-center justify-center gap-3 text-sm text-white/50"><Camera size={17} /> Camera is ready</div></section><aside className="rounded-[24px] bg-spot-ink p-6"><div className="flex items-center gap-3 border-b border-white/10 pb-5"><span className="grid h-10 w-10 place-items-center rounded-full bg-spot-lime text-spot-ink"><ShieldCheck size={19} /></span><div><p className="font-bold">Doors are open</p><p className="mt-1 text-xs text-white/40">Saturday, 16 Jun · 10:00 AM</p></div></div><div className="py-6"><div className="flex justify-between text-sm"><span className="text-white/45">Checked in</span><span className="font-bold">48 / 100</span></div><div className="mt-3 h-2 rounded-full bg-white/10"><div className="h-full w-[48%] rounded-full bg-spot-blue" /></div></div><div className="border-t border-white/10 pt-5"><label className="mb-2 block text-xs font-bold uppercase tracking-wider text-white/45">Manual code</label><div className="flex gap-2"><input value={code} onChange={(e) => setCode(e.target.value)} placeholder="e.g. SPOT-2938" className="min-w-0 flex-1 rounded-xl bg-white/10 px-3 py-3 text-sm text-white outline-none placeholder:text-white/25 focus:ring-2 focus:ring-spot-lime" /><button onClick={verify} className="rounded-xl bg-white px-3 text-spot-ink" aria-label="Verify code"><Keyboard size={17} /></button></div><button onClick={verify} className="mt-3 w-full rounded-xl border border-white/15 py-3 text-sm font-bold hover:bg-white/10">Check ticket</button></div></aside></div>{status !== 'idle' && <div className={`fixed inset-x-5 bottom-5 mx-auto flex max-w-md items-center gap-4 rounded-2xl p-4 shadow-2xl ${status === 'valid' ? 'bg-spot-lime text-spot-ink' : 'bg-spot-coral text-white'}`}>{status === 'valid' ? <Check /> : <X />}<div><p className="font-bold">{status === 'valid' ? 'Ticket valid — welcome in!' : 'Ticket not found'}</p><p className="mt-0.5 text-xs opacity-70">{status === 'valid' ? 'General admission · SPOT-2938' : 'Try scanning again or check the code.'}</p></div><button onClick={() => setStatus('idle')} className="ml-auto opacity-60">×</button></div>}</div></div>
}
